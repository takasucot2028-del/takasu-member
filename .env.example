# GAS Web App のデプロイURL。
# 設定すると本番（GAS）モード、未設定なら localStorage デモモードで動作する。
# このファイルをコピーして .env を作成し、実際のURLを設定する（.env はコミットされない）。
VITE_GAS_URL=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec
